services:
  nginx:
    build:
      context: requirements/nginx
    container_name: nginx
    depends_on:
      - wordpress
    networks:
      - inception
    ports:
      - "443:443"
    restart: always

  wordpress:
    build:
      context: requirements/wordpress
    container_name: wordpress
    networks:
      - inception
    volumes:
      - wp_vol:/var/www/html

networks:
  inception:
    driver: bridge

volumes:
  db_vol:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/acourtar/data/mariadb
  wp_vol:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/acourtar/data/wordpress
