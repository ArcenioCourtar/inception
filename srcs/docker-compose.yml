services:
  nginx:
    build:
      context: requirements/nginx
    container_name: nginx
    networks:
      - inception
    ports:
      - "443:443"
    restart: always

networks:
  inception:
    driver: bridge
