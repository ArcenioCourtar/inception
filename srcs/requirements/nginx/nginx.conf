worker_processes 1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 443;

        location / {
            return 200 "It's working chat\n";
        }

		location ~ \.php$ {
			include fastcgi_params;
			fastcgi_pass wordpress:9000;
		}
    }
}
