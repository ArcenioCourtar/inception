USEFUL:
docker logs [container name] to see messages printed by container while running
"nc -zv [container name] [port]" To check ports in the network when using docker
interactive mode
I made some changes to me zshrc and theme for work on inception.
Storing them here in case I need to set up a new VM :)

.zshrc:
alias mountinception="sudo mount -t vboxsf inception /mnt/inception"
mytotallyoriginaltheme.zsh-theme:
PROMPT+="$(echo $USER)${NEWLINE}"

After setting up the new VM with more storage (30GB instead of 10), it works YAAAAY
I tried increasing the size of the VDI but that led to partitioning hell :(

TODO:
Howto https. (currently can only connect through http)

REQUIREMENTS:
Makefile that sets up entire application with a docker-compose.yml

Call the Dockerfiles for each service using docker-compose
docker-network establishes the connections between containers
containers need to restart upon crash

Two named volumes: one for your WP database and another for WP website files
These names volumes must be stored on /home/[login/data on the host machine
Store variables in a .env

NGINX:
set up nginx server with TLSv1.2/1.3

WordPress + php-fpm:

MariaDB:
